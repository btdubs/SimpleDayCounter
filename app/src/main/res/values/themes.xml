<?xml version="1.0" encoding="utf-8"?>
<resources>

  <style name="Theme.SimpleDayCounter" parent="Theme.SimpleDayCounterBase">
    <item name="android:windowLightStatusBar">true</item>
  </style>

  <style name="Theme.SimpleDayCounterBase" parent="Theme.Material3.DayNight.NoActionBar">
    <item name="colorPrimary">@color/primary</item>
    <item name="colorAccent">@color/accent</item>
  </style>

  <!-- Android's Theme.NoDisplay but without <item name="windowNoDisplay">true</item> that requires
   calling finish() before onResume() completes.-->
  <style name="Theme.AlmostNoDisplay" parent="@android:style/Theme">
    <item name="android:windowBackground">@null</item>
    <item name="android:windowContentOverlay">@null</item>
    <item name="android:windowIsTranslucent">true</item>
    <item name="android:windowAnimationStyle">@null</item>
    <item name="android:windowDisablePreview">true</item>
  </style>
</resources>
